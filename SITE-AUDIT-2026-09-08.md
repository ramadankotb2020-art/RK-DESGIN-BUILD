# مراجعة موقع RK Design

تاريخ المراجعة: 8 سبتمبر 2026

## نطاق المراجعة
مراجعة الكود الموجود في المستودع، مطابقة مسارات الملفات، واختبارات محلية مباشرة لبعض دوال API. لم يتم اختبار الاستضافة الفعلية أو الدخول إلى حساب الإدارة أو إجراء تعديلات على بياناتها. تعذر تنزيل Chromium بسبب خطأ شبكة؛ لذلك لا يتضمن التقرير قياسات Lighthouse أو حكمًا بصريًا مؤكدًا على الموبايل. لم تُعدّل ملفات تشغيل الموقع.

## الخلاصة
الموقع لديه أساس جيد كبورتفوليو عربي: RTL، أقسام أعمال، صور WebP، بحث وفلاتر، تواصل واتساب ومخطط غرف. لكن دورة إدارة المحتوى غير مكتملة، وهناك خلل أمني مشروط بإعدادات الاستضافة، ومشكلات في الملفات وSEO. الأولوية لإصلاح الوظائف والأمان قبل إضافة مؤثرات أو إعادة تصميم.

## 1. عاجل — المصادقة تقبل جلسة قابلة للاشتقاق إذا لم تُضبط كلمة السر
- المرجع: `functions/_utils.js`، الدالتان `sessionToken` و`isAuthenticated`.
- تسجيل الدخول يرفض غياب `ADMIN_PASSWORD`، لكن التحقق من الجلسة لا يرفضه؛ بل يحسب قيمة ثابتة باستخدام نص فارغ.
- اختبار محلي باستخدام بيئة بدون كلمة سر أعاد `true` من التحقق من المصادقة. التأثير على البيانات يتوقف على وجود بقية إعدادات الخدمات. لا يعني هذا إثبات أن النسخة المنشورة مخترقة أو أن كلمة سرها مفقودة.
- كذلك الجلسة مشتقة بصورة ثابتة من كلمة السر، بلا صلاحية يتحقق منها الخادم أو إبطال فردي. حذف الكوكيز عند الخروج لا يبطل نسخة مسروقة منها. لم يظهر محدد لمحاولات الدخول في الكود؛ قد توجد حماية خارجية في Cloudflare لم تُراجع.
- العلاج: رفض أي مصادقة عند غياب الإعداد، جلسات عشوائية محددة الصلاحية وقابلة للإبطال، وتحديد محاولات الدخول.

## 2. مرتفع — حفظ مشروع من لوحة التحكم يفشل
- المرجع: `admin.html:213` و`functions/api/projects/index.js`.
- الواجهة ترسل العنوان والقسم والتصنيف والغلاف والمعرض فقط، بينما الخادم يشترط أيضًا `excerpt` و`description`.
- إعادة تنفيذ نفس بيانات الطلب محليًا أعادت HTTP 400 ورسالة «فيه حقول أساسية ناقصة».
- العلاج: توحيد مخطط البيانات وإضافة الحقول اللازمة للواجهة أو تعريف قيم افتراضية مناسبة في الخادم.

## 3. مرتفع — زر استيراد البيانات يفشل
- المرجع: `admin.html:182` و`functions/api/seed.js`.
- الزر يرسل POST بدون body، والخادم ينتظر مصفوفة JSON.
- الاختبار المحلي أعاد HTTP 400 ورسالة «بيانات غير صالحة».
- العلاج: إرسال مصفوفة المشاريع فعليًا مع Content-Type، وإظهار رسالة الخادم للمستخدم.

## 4. مرتفع — تعديلات لوحة التحكم لا تصل تلقائيًا إلى صفحات الموقع
- المرجع: نهاية `js/projects-data.js` وبداية `js/projects.js`.
- واجهة الموقع تستخدم `const projectsData = PROJECTS_FALLBACK`؛ لا يوجد في مسار العرض طلب إلى `/api/projects`.
- لوحة التحكم تكتب إلى KV، بينما الموقع يقرأ ملفًا ثابتًا. حتى بعد إصلاح الحفظ، ستبقى المشاريع الجديدة غير ظاهرة دون تعديل تدفق القراءة أو إعادة بناء الموقع.
- العلاج: اعتماد مصدر بيانات واحد، إما API مع fallback عند الفشل، أو عملية نشر/بناء مرتبطة بالتحديثات.

## 5. مرتفع — توقيع رفع الصور لا يشمل كل معاملات الطلب
- المرجع: `functions/api/upload.js`.
- النص الموقع يحتوي `folder` و`timestamp` فقط، ثم تُضاف `transformation=q_auto,f_auto,w_1600` إلى طلب الصور بعد التوقيع.
- معاملات التحويل يجب أن تدخل ضمن معاملات توقيع Cloudinary؛ عدم التطابق متوقع أن يرفض رفع الصور. لم يُجرَ رفع حقيقي لعدم استخدام أسرار الخدمة.
- العلاج: تجميع معاملات الرفع أولًا ثم حساب التوقيع عليها، واختبار صورة وفيديو منفصلين.

## 6. متوسط إلى مرتفع — إدخال بيانات المشاريع مباشرة داخل HTML
- المرجع: `js/projects.js` في البطاقات والفلاتر والمعرض، و`js/main.js` في البحث.
- قيم مثل عنوان المشروع والتصنيف ومسار الصورة تدخل في `innerHTML` بلا escaping. تحديث المشروع يقبل حقولًا بشكل واسع في `functions/api/projects/[id].js`.
- هذا نمط يسمح بـ stored XSS عند عرض بيانات خبيثة. حاليًا واجهة العرض ثابتة، لذا لا يصح اعتبار API مسار استغلال مباشر للواجهة قبل ربطهما؛ الخطر يصبح أوضح عند تنفيذ هذا الربط.
- العلاج: بناء العناصر باستخدام DOM و`textContent`، فحص بروتوكولات روابط الوسائط، والتحقق من الأنواع والحقول المسموحة في الخادم.

## 7. متوسط — مراجع وسائط وملفات مفقودة
- فحص بيانات 52 مشروعًا وجد 29 مرجعًا إلى ملفات غير موجودة على القرص، منها العديد من مسارات `cover.mp4`.
- `js/projects.js` يخمن أيضًا مسار `cover.mp4` عندما لا توجد فيديوهات بالمعرض، ثم يخفي العنصر عند الفشل. هذا ينتج طلبات فاشلة بدل تحديد الوسائط المتوفرة فعلًا.
- يوجد مرجع `favicon.ico` مفقود في سبع صفحات؛ أيقونة SVG موجودة، لذلك لا يعني ذلك غياب الأيقونة بالكامل.
- صفحة المخطط تشير إلى `manifest.webmanifest` و`assets/pwa/icon-192.png` غير الموجودين، ما يجعل إعداد PWA غير مكتمل.
- العلاج: توليد بيانات الوسائط من الملفات الفعلية وفحص المسارات قبل النشر، وإزالة تخمين الفيديوهات.

## 8. متوسط — فهرسة المشاريع ومشاركتها غير مميزتين
- المرجع: `project.html:10–20` و`sitemap.xml`.
- كل المشاريع تستخدم canonical وog:url عامين إلى `project.html` بلا معرف المشروع، وعنوان وصورة مشاركة عامين. JavaScript يغير عنوان المتصفح فقط.
- sitemap لا يتضمن روابط المشاريع الفردية. غيابها وحده لا يمنع الفهرسة، لكنه لا يساعد اكتشاف الأعمال.
- الرابط ذو المعرف المفقود أو غير الصحيح يترك نص «عنوان المشروع» و«تفاصيل المشروع...» لأن دالة العرض تعود مبكرًا بلا حالة خطأ.
- يوجد اختلاف بين دومين `rk-design.com` في SEO وروابط `pages.dev` المثبتة في المخطط؛ يلزم التحقق من الدومين الإنتاجي المعتمد قبل التغيير.
- العلاج: صفحات مشاريع مولدة مسبقًا ببيانات وصفية وصور وcanonical مخصصة، وروابط ضمن sitemap، وحالة مشروع غير موجود مع عودة واضحة للأعمال.

## 9. متوسط — الأداء يحتاج ترشيدًا لا حكمًا من حجم المستودع فقط
- مجلد الصور والوسائط داخله نحو 682 MiB. هذا حجم على القرص، وليس حجم تنزيل الصفحة الواحدة ولا قياس سرعة.
- `js/home.js` يقرأ خيار توفير البيانات `saveData` لكنه لا يستخدمه لمنع الفيديو، ويجهز خلفيات الشرائح من البداية.
- فيديوهات معرض المشروع تستخدم autoplay وloop بلا تأجيل واضح حسب ظهورها على الشاشة.
- صفحة `room-designer.html` نحو 366 KiB وفهرس الأصول نحو 720 KiB على القرص، ما يزيد عبء الصيانة والتحميل الأولي المحتمل.
- توجد نقاط جيدة: WebP، lazy loading لصور البطاقات، واحترام تقليل الحركة في بعض المؤثرات.
- العلاج: اختبار تحميل فعلي بشبكة موبايل بطيئة، احترام توفير البيانات، تحميل الشرائح التالية عند الحاجة، وتأجيل الفيديوهات غير المرئية. لا توجد قياسات LCP/CLS/INP مؤكدة في هذه المراجعة.

## 10. متوسط — محتوى المشاريع لا يشرح قيمة الشغل بما يكفي
- 38 من 52 مشروعًا بدون فكرة ومحتواها الوصفي فارغ أو مجرد «مشروع [الاسم]» بحسب فحص البيانات.
- 35 مشروعًا تصنيفها فارغ؛ هذا يقلل فائدة الفلاتر ويجعل عرض الأعمال أقل وضوحًا.
- العلاج: لكل مشروع مهم أضف المطلوب من العميل، التحدي، دورك، الحل، والمخرجات الفعلية؛ مع المساحة والموقع والمدة إذا كانت معلومات متاحة وصحيحة. ابدأ بأفضل 6–10 مشاريع بدل كتابة وصف عام للجميع.

## 11. متوسط — تفاصيل التواصل وإتاحة الاستخدام
- نموذج التواصل يعمل بتحويل إلى واتساب، وليس استقبالًا أو حفظًا لرسالة داخل الموقع. README الذي يقول إنه واجهة بلا إرسال أصبح غير دقيق.
- حقل الخدمة معلّم كإلزامي، لكن التحقق في `js/main.js` لا يشترطه. الهاتف يُفحص لعدم الفراغ فقط.
- فتح واتساب مؤجل بـ setTimeout ولا توجد معالجة إذا حظر المتصفح النافذة؛ احتمالية تحتاج اختبار متصفحات.
- البحث لديه `role=dialog`، لكن لا يوجد حصر للتركيز داخل النافذة أو استرجاعه عند الإغلاق. نافذة الصور ينقصها توصيف dialog وتسميات واضحة لأزرارها، وفتح الصور يتم عبر div قابل للنقر وليس زرًا قابلًا للوصول بالكيبورد.
- العلاج: توضيح «متابعة عبر واتساب»، التحقق من المدخلات، رابط بديل عند تعذر الفتح، وإكمال دعم لوحة المفاتيح والتركيز وقارئات الشاشة.

## 12. صيانة واختبارات
- لا توجد أوامر test أو lint ضمن package.json. توجد ملفات نسخ احتياطية داخل مجلد النشر المحتمل، وتعليمات README قديمة لا تعكس لوحة التحكم الحالية.
- جميع المشاريع تُخزن في مفتاح KV واحد مع read-modify-write، ما يعرض التعديلات المتزامنة لفقد آخر تحديث. الأهمية تزيد لو أصبح هناك أكثر من محرر أو طلبات متزامنة.
- العلاج: اختبارات عقود بين الواجهة وAPI، اختبار المسارات وبنية بيانات المشاريع، تحديث تعليمات النشر، واستبعاد ملفات الصيانة والنسخ الاحتياطية من النشر العام.

## ترتيب التنفيذ المقترح
1. سد مشكلة المصادقة والجلسات.
2. إصلاح حفظ واستيراد ورفع المشاريع، واختبار الدورة كاملة.
3. توحيد مصدر بيانات الواجهة والإدارة مع حماية إخراج HTML.
4. تنظيف المراجع المفقودة وحالة المشروع غير الموجود.
5. تحسين SEO وأوصاف أفضل المشاريع.
6. اختبار بصري ووظيفي على الموبايل والكمبيوتر، وقياس الأداء والإتاحة على الاستضافة الفعلية، ثم ضبط المؤثرات والتصميم بناءً على النتائج.
